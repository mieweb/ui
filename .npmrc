# Required: pnpm v10 disables pre/post lifecycle scripts by default.
# The preinstall/postinstall hooks in package.json auto-init the datavis
# git submodule so fresh clones can build without manual setup.
enable-pre-post-scripts=true

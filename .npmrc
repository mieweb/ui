# Required: pnpm v10 disables pre/post lifecycle scripts by default.
# The preinstall hook in package.json auto-inits the eSheet and YChart
# git submodules so fresh clones can build without manual setup.
enable-pre-post-scripts=true

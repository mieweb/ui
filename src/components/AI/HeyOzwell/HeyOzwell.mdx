import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as HeyOzwellStories from './HeyOzwell.stories';

<Meta title="Modules/Voice/Overview" />

# Hey Ozwell

Hey Ozwell is the **voice entry point** for the assistant: wake word, speaker
verification, dictation, hands-free chat, and ambient visit scribing, all
running **on-device, in the browser** by default. In that default
configuration **audio never leaves the page**, which makes the stack PHI-safe
out of the box. Two things do cross the network: the model assets on first
load (downloaded once, then cached locally, see _Dependencies & hosting_) and
the assistant's text reply (any OpenAI-compatible endpoint). One opt-in
setting changes the audio guarantee: `transcription="server"` POSTs the clip
to your own ASR endpoint instead of running Whisper in the browser.

**80-second walkthrough** of everything on this page:

<iframe
  width="315"
  height="560"
  src="https://www.youtube.com/embed/hQBGofjXqmg"
  title="Hey Ozwell Demo"
  loading="lazy"
  frameBorder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
  allowFullScreen
  style={{ borderRadius: 12, maxWidth: '100%' }}
/>

If the embed doesn't load in your environment:
[watch on YouTube](https://www.youtube.com/shorts/hQBGofjXqmg).

## Which one?

> **Where does voice enter, and who is speaking?** A header toggle that opens a floating chat
> → `HeyOzwell`. The assistant _is_ the page → `HandsFreeChat`. Several people in a room to
> transcribe, no assistant turns → `VisitScribe`. Enrolling or managing voices → `VoiceSetup` /
> `VoiceManager`. Building your own flow → the `Wake Word` and `Speaker Verify` primitives. Just a
> mic button on a chat → [AIChat (Voice)](?path=/docs/chat-aichat-voice--docs) in Chat.

| Component                                                  | Use it when                                                                                                                                                                          | Not when                                                                                                                                                                  |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Hey Ozwell Demo](?path=/docs/voice-hey-ozwell-demo--docs) | Drop-in for the app header: octopus toggle + settings menu + `FloatingAIChat`; wake → dictate → transcribe → send; `requireDoctor`, `conversationMode`, `reviewBeforeSend`, `onSend` | The chat must be inline — `HandsFreeChat`; you already render `AIChat` — `useHeyOzwell` + your layout; the device cannot download models                                  |
| [Hands-Free Chat](?path=/docs/voice-hands-free-chat--docs) | A full-height, always-listening assistant page (kiosk, dictation workstation); the same flow as an inline `AIChat`; reference composition of the primitives                          | Voice should be opt-in from the header — `HeyOzwell` (no mic until activated); you need `onSend` to your own backend (use the hook)                                       |
| [Visit Scribe](?path=/docs/voice-visit-scribe--docs)       | Record a multi-person encounter, stop, get an on-device speaker-labelled transcript; rename/merge/reassign speakers; Re-analyze with new settings                                    | You only need the audio file — [AudioRecorder](?path=/docs/media-audiorecorder--docs); it is a dialogue with the assistant — `HandsFreeChat`; server-side ASR is mandated |
| [Voice Manager](?path=/docs/voice-voice-manager--docs)     | The "Your voice" settings page: list, rename, remove, add and clear enrolled voices; launches `VoiceSetup`; target of `HeyOzwell onManageVoices`                                     | Enrollment embedded in onboarding — `VoiceSetup` alone; inspecting scores — `Speaker Verify`; voices must sync across devices (per-browser IndexedDB only)                |
| [Voice Setup](?path=/docs/voice-voice-setup--docs)         | The guided enrollment screen (two phrases × 3 reps) building the WHO speaker print and WHAT phrase print; `mode="add"` for another voice or condition                                | You need to see what is enrolled — `VoiceManager`; a compact control (this is a full-bleed page); the user cannot say the fixed English phrases                           |
| [Wake Word](?path=/docs/voice-wake-word--docs)             | Headless `useWakeWord` for a custom voice surface: `onWake`, `onUtterance`, thresholds, the shared `getStream()`; `warmWakeModels` prefetch                                          | You want the finished flow — `HeyOzwell` / `HandsFreeChat`; you need _who_ spoke — pair with `Speaker Verify`; different or localised phrases (new models)                |
| [Speaker Verify](?path=/docs/voice-speaker-verify--docs)   | **Developer diagnostic** (experimental) for tuning the WHO/WHAT gates with live scores; reference for `useSpeakerVerify` (`verify`, `identify`, `enroll`, `setGates`)                | End users enrolling — `VoiceSetup` / `VoiceManager`; identity as an authentication factor (it is a convenience gate)                                                      |

## Quick start

```tsx
import { HeyOzwell } from '@mieweb/ui';

// Drop the octopus into your header. That's the whole integration.
<HeyOzwell chatProps={{ userName: 'Dr. Jane' }} />;
```

Click the octopus to turn Ozwell on, say **"hey ozwell"** to dictate, and
**"ozwell I'm done"** to transcribe and send. For custom layouts, use the
headless hook and compose the parts yourself:

```tsx
import { useHeyOzwell, HeyOzwellToggle } from '@mieweb/ui';

const oz = useHeyOzwell({ autoDictateOnWake: true, requireDoctor: true });
// oz.toggleProps → <HeyOzwellToggle>, oz.chatProps → <FloatingAIChat>, plus
// send/startDictation/stopDictation for fully custom surfaces.
```

## The flow

1. **The octopus lives in the top bar.** Off, it sits gray and muted.
2. **Click to activate.** A ring fills while the wake detector starts, and it
   completes **green the moment the mic is live**: ring done means "you can
   talk now". The heavier transcription model keeps loading on a thin outer
   arc, but it never gates you. Audio buffers and the transcript catches up.
3. **Say "hey ozwell".** The chat opens and (with auto-dictate) starts
   recording. The octopus pulses with the room volume.
4. **Say "ozwell I'm done".** The clip is trimmed so the stop phrase never
   reaches the transcript, transcribed on-device, and sent. The reply streams
   into the floating chat.
5. **Turn Ozwell off** and the mic is released. A gray octopus means the mic
   is genuinely off.

<Canvas of={HeyOzwellStories.Interactive} />

### The header octopus across its states

Off (gray) → active and quiet → active and hearing speech:

<Canvas of={HeyOzwellStories.ToggleStates} />

## Options

All options are props on `<HeyOzwell>` / `useHeyOzwell` (live in the Demo's
Controls panel):

- **`autoDictateOnWake`**: ON, "hey ozwell" opens the chat **and** starts
  dictating. OFF, it just opens the chat and waits.
- **`requireDoctor`**: the speaker-verify gate. Every wake is checked
  invisibly against the **enrolled clinician's voiceprint** (WHO) and a
  phrase-print (WHAT); anyone else is ignored. Stays open until a voice is
  enrolled.
- **`transcription`**: `'browser'` (default) runs Whisper on-device in a
  **Web Worker**. `'server'` POSTs audio to an OpenAI-compatible
  `/v1/audio/transcriptions` endpoint (audio leaves the browser; falls back
  to on-device on failure).
- **`conversationMode`**: on "done", **diarize** the clip and send
  who-said-what ("Dr. Jane: … / Patient: …") instead of a flat transcript.
- **`liveTranscript`**: show a rough real-time caption in the composer while
  dictating. The final send still re-transcribes the full clip.
- **`reviewBeforeSend`**: drop the finished transcript into the composer to
  glance and edit before sending, instead of auto-firing.
- **`closeChatOnDone`**: close the chat popup after the dictated message
  sends.
- **`onSend`**: route sends to your own backend or message store instead of
  the built-in client.
- **`assetBase`**: override where the wake/speaker models load from.

Right-click (or long-press) the octopus for **Ozwell settings**: voice
enrollment and the live "Models & versions" readout.

## Visit Scribe (ambient scribe)

The second surface, for **multi-person encounters**. No wake word, no turns:
record the room, stop, and get an **on-device speaker-labeled transcript**
(Doctor / Patient / Nurse). Under the hood it's Whisper timestamps plus
TitaNet speaker embeddings, clustered into turns and anchored to enrolled
voiceprints, with optional LLM role inference. Rename, merge, and reassign
speakers afterward, and optionally show a rough live transcript while
recording. Same guarantee: audio never leaves the page. See _Visit Scribe_
in this section.

## How it works: what runs where

One shared mic: the wake detector opens `getUserMedia` once and every other
consumer (dictation recorder, volume meter, verification buffer) reads from
`getStream()`. A second `getUserMedia` would silence the detector.

- **Mic capture + 16 kHz resample**: AudioWorklet, on the audio thread.
- **Wake word** ("hey ozwell" / "ozwell i'm done"): ONNX via onnxruntime-web
  (~6 MB of models), on the main thread.
- **Speaker verification** (the WHO gate, AS-norm scoring): sherpa-onnx +
  TitaNet (~50 MB), on the main thread.
- **Transcription**: Whisper turbo (~1.3 GB, WebGPU; base.en WASM fallback)
  via transformers.js, in a **Web Worker**.
- **Diarization**: Whisper timestamps + TitaNet embeddings, worker + main.
- **Assistant reply**: OpenAI-compatible `/v1/chat/completions` (SSE
  streaming). This is the only network stage at runtime, unless you opt into
  `transcription="server"`.

Transcription lives in a Web Worker so downloading and compiling the 1.3 GB
model never starves the main-thread wake detector. **"Hey ozwell" responds
while Whisper is still loading**, and the buffered audio catches up.

### Models, caching, hosting

See _Dependencies & hosting_ below for the full table. In short: wake models are
cached in OPFS, the speaker runtime and Whisper weights in the Cache API, and
every host is config-swappable.

## Dependencies & hosting

Nothing in this family is bundled except the React code and `onnxruntime-web`
(a runtime dependency of `@mieweb/ui`). Everything else is **fetched at runtime**
from configurable hosts and cached in the browser. The defaults point at a
personal HuggingFace repo (`jlocala/ozwell-voice-assets`), a Cloudflare R2
bucket and jsDelivr — fine for the Storybook demo, **not** for production. Set
the overrides before the components mount (app entry or `.storybook/preview`).

| Asset                                                                                                                                | Size    | Used by                                                                                               | Runs                                                               | Default host → override                                                                                                                                                                 | Cache                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------ | ------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **Wake-word models** — `hey-ozwell.onnx`, `ozwell-i'm-done.onnx`, `silero-vad.onnx`, `speech-embedding.onnx`, `mel-spectrogram.onnx` | ~6 MB   | `useWakeWord` → `HeyOzwell`, `HandsFreeChat`, `VoiceSetup`, `Speaker Verify`                          | Main thread, `onnxruntime-web`                                     | `<base>/wakeword/` → `assetBase` prop, `window.__ozwellAssets` (string or `{ wakeword, svRuntime }`), `localStorage.ozwellAssetBase`                                                    | OPFS (`warmWakeModels` prefetches); Cache API via service worker                                 |
| **onnxruntime-web 1.19** (`ort.min.js`)                                                                                              | ~few MB | `useWakeWord`                                                                                         | Main thread, WASM                                                  | jsDelivr CDN; falls back to the bundled ESM if the CDN load fails                                                                                                                       | Browser HTTP cache                                                                               |
| **Speaker runtime** — sherpa-onnx WASM + `.data` (TitaNet `titanet_small`) + `sv-cohort.json` (AS-norm cohort)                       | ~50 MB  | `useSpeakerVerify` → `requireDoctor`, `conversationMode`, `VisitScribe`, `VoiceSetup`, `VoiceManager` | Main thread, WASM, via `<script>` globals (`window.SpeakerVerify`) | `<base>/sv-runtime/` → `window.__ozwellAssets`; pass `useSpeakerVerify({ enabled: false })` to skip when the gate is off                                                                | Cache API via service worker                                                                     |
| **Whisper turbo** (encoder fp16 / decoder q4, multilingual)                                                                          | ~1.3 GB | `transcribeBlob` / `transcribeSamples` → dictation, live captions, diarization                        | **Web Worker**, WebGPU (WASM fallback)                             | Cloudflare R2 → `window.__ozwellWhisperHost` / `ozwellConfig.whisperHost` (host must send `Content-Length`); `ozwellConfig.whisper: 'base.en'` selects the ~75 MB English model instead | Cache API, streamed by Transformers.js (`navigator.storage.persist()` requested, not guaranteed) |
| **Whisper base.en** (`Xenova/whisper-base.en`)                                                                                       | ~75 MB  | Stop-phrase confirmation gate (`transcribeGate`, `warmStopGate`); turbo fallback                      | Web Worker                                                         | HuggingFace → same overrides                                                                                                                                                            | Cache API                                                                                        |
| **Transformers.js 3.8.1** ESM                                                                                                        | ~1 MB   | The Whisper worker                                                                                    | Web Worker (`import()` from a Blob-URL module)                     | jsDelivr → `window.__ozwellTransformersUrl` / `localStorage.ozwellTransformersUrl`                                                                                                      | Browser HTTP cache                                                                               |
| **Model service worker** `ozwell-model-sw.js`                                                                                        | —       | Caches the wake + speaker files across app opens                                                      | Service worker                                                     | Served by Storybook from `.storybook/public/`; a consumer must copy it to its own origin or pass `useWakeWord({ registerServiceWorker: false })`                                        | —                                                                                                |

**Browser vs server transcription.** By default (`transcription="browser"`) every
stage — wake, verification, Whisper, diarization — runs in the browser and no
audio leaves the page. `transcription="server"` (`HeyOzwell`, `HandsFreeChat`)
instead records the clip and POSTs it to `/v1/audio/transcriptions` on the
configured OpenAI-compatible base URL (`transcribeServer`), falling back to
on-device on failure; `conversationMode` overrides it because diarization needs
the audio locally. `VisitScribe` has no server option. The assistant **reply**
(`askOzwellStream`) and `VisitScribe`'s optional role inference (`askOzwell`,
transcript text only) are the only other network stages, and only when
`ozwellConfig` / `window.__ozwell` holds a key.

**Browser requirements.** Secure context (HTTPS or `localhost`), microphone
permission, AudioWorklet, Web Workers, WASM, IndexedDB (voiceprints, db
`ozwell-voice`), OPFS; WebGPU strongly recommended for turbo (set
`window.__ozwell.whisperDevice = 'wasm'` to skip the attempt). Strict CSP needs
`worker-src blob:` plus the model and CDN origins — or self-host everything via
the overrides above. Peer/package facts: `onnxruntime-web` is in
`dependencies`; sherpa-onnx, Whisper and Transformers.js are **not** npm
dependencies at all. Voiceprints persist per browser and origin; `localStorage`
holds config only.

## Backend integration

The component is backend-agnostic. Two seams:

- **`onSend` prop**: the hook hands you the final text. Call whatever backend
  you like and render the reply yourself.
- **`ozwellConfig`**: leave `onSend` off and the built-in client streams from
  any OpenAI-compatible endpoint. Configure via `localStorage.ozwellConfig`
  or `window.__ozwell` with `{ apiKey, baseURL, model, system, temperature }`.
  The sentinel `apiKey: 'ollama'` targets a local Ollama server (no auth
  header). With no key configured, the demo falls back to a canned reply, so
  no secret is ever committed.

> ⚠️ A browser-held Bearer key is visible to anyone with the page. For public
> deploys, proxy the key server-side and point `baseURL` at the proxy. See
> `src/components/AI/OZWELL-BACKEND.md`.

## Troubleshooting

- **Octopus ring never completes**: mic permission denied or no input
  device. The toggle surfaces "Microphone unavailable" after ~8 s.
- **First activation is slow**: the Whisper download (up to ~1.3 GB) happens
  once, then it's served from the Cache API. The wake word works during the
  download; only transcription waits.
- **Strict CSP hosts**: the worker needs `worker-src blob:`, plus the model
  CDN allowances (or self-host everything via the overrides above).
- **"hey ozwell" ignored with the doctor gate on**: re-enroll in conditions
  matching real use (same mic, same room). The WHO gate is tunable via
  `window.SpeakerVerify` thresholds.
- **Console `[W:onnxruntime …]` messages during model load**: harmless ONNX
  runtime warnings about CPU-assigned graph nodes, logged at error level.

## Related

- [Voice Setup](?path=/docs/voice-voice-setup--docs): one-time speaker enrollment (the octopus-pulse pattern).
- [Voice Manager](?path=/docs/voice-voice-manager--docs): manage, rename, and remove enrolled voices.
- [Wake Word](?path=/docs/voice-wake-word--docs): the on-device detector primitive.
- [Speaker Verify](?path=/docs/voice-speaker-verify--docs): developer diagnostic for the WHO/WHAT gates.
- [Hands-Free Chat](?path=/docs/voice-hands-free-chat--docs): the voice conversation composition.
- [Visit Scribe](?path=/docs/voice-visit-scribe--docs): the ambient multi-person scribe.
- [Chat › Overview](?path=/docs/modules-chat-overview--docs): the `AIChat` these surfaces render into, and
  [AIChat (Voice)](?path=/docs/chat-aichat-voice--docs) for push-to-talk without wake words.

Deeper docs in the repo: `MODEL-HOSTING.md` (asset hosting + cutover),
`OZWELL-BACKEND.md` (chat wiring), `DIARIZATION.md` (speaker attribution).

/** Type declarations for the vendored speaker-verification runtime (speaker-verify.js). It's a
 *  side-effect module — running it sets `window.SpeakerVerify`; no direct exports are consumed. */
export {};

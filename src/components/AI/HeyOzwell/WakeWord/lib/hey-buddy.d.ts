/** Type declarations for the vendored vanilla-JS wake-word detector (hey-buddy.js). The consumer casts
 *  the constructed instance to a richer local shape; this just gives the module + constructor a type. */
export const HeyBuddy: new (opts: unknown) => unknown;
